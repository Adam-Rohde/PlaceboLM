% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression.R
\name{plm_analytic}
\alias{plm_analytic}
\title{Analytic standard error and interval for the adjusted estimate}
\usage{
plm_analytic(
  fit,
  m = NULL,
  k = NULL,
  imperfection = 0,
  alpha = 0.05,
  vcov = NULL
)
}
\arguments{
\item{fit}{A \code{placebo_lm} object with structure \code{"placebo_outcome"}.}

\item{m, k}{Numeric scalar relative-confounding parameter. Supply one.}

\item{imperfection}{Numeric scalar. Postulated placebo imperfection.}

\item{alpha}{Numeric. Significance level. Defaults to \code{0.05}.}

\item{vcov}{Optional function taking the fitted model and returning a
variance-covariance matrix, e.g. \code{sandwich::vcovHC} or a call to
\code{sandwich::vcovCL}. Defaults to \code{\link[stats:vcov]{stats::vcov()}}.}
}
\value{
A one-row data frame with columns \code{m}, \code{k}, \code{imperfection},
\code{estimate}, \code{std_error}, \code{ci_lower}, and \code{ci_upper}.
}
\description{
Exact analytic inference for the \code{m} parameterization, via the regression
representation of \code{\link[=plm_regression]{plm_regression()}}. Conditional on the postulated
sensitivity parameters, the adjusted estimate is an ordinary regression
coefficient, so its standard error is available in closed form rather than by
resampling.

Supply \code{vcov} to use a heteroskedasticity- or cluster-robust variance
estimator; the default is the classical one.

When reasoning with \code{k}, the scale factor is itself estimated and these
intervals condition on it. The paper recommends the nonparametric bootstrap
in that case — see \code{\link[=plm_grid]{plm_grid()}}.
}
\examples{
set.seed(1)
n <- 400
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + U + rnorm(n)
fit <- placebo_lm(data.frame(Y = Y, D = D, P = P),
                  "Y", "D", "P", structure = "placebo_outcome")

plm_analytic(fit, m = 1)

}

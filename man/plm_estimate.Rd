% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{plm_estimate}
\alias{plm_estimate}
\title{Partially identified estimate at given sensitivity parameters}
\usage{
plm_estimate(fit, k = NULL, m = NULL, imperfection = 0)
}
\arguments{
\item{fit}{A \code{placebo_lm} object from \code{\link[=placebo_lm]{placebo_lm()}}.}

\item{k}{Numeric. Scale-free relative confounding. Supply exactly one of
\code{k} or \code{m}.}

\item{m}{Numeric. Raw ratio of biases. Conventional difference-in-differences
is \code{m = 1}.}

\item{imperfection}{Numeric. The postulated value of the structure's
sensitivity parameter (see \code{fit$spec$sens_param}); zero means a perfect
placebo. Defaults to \code{0}.}
}
\value{
A numeric vector of adjusted estimates.
}
\description{
Evaluates the adjusted causal estimate at postulated values of the
relative-confounding parameter (\code{k} or \code{m}) and the placebo imperfection.

Setting \code{k = 0} gives the selection-on-observables estimate (no unobserved
confounding). Setting \code{m = 1} with \code{imperfection = 0} gives the conventional
difference-in-differences estimate. Setting \code{k = 1} gives equiconfounding
after rescaling.

Arguments are vectorised and recycled, so a whole grid can be evaluated in
one call.
}
\examples{
set.seed(1)
n <- 500
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + U + rnorm(n)
fit <- placebo_lm(data.frame(Y = Y, D = D, P = P),
                  "Y", "D", "P", structure = "placebo_outcome")

plm_estimate(fit, k = 0)   # no unobserved confounding
plm_estimate(fit, m = 1)   # conventional difference-in-differences
plm_estimate(fit, k = 1)   # equiconfounding after rescaling
plm_estimate(fit, k = seq(0, 2, by = 0.5))

}

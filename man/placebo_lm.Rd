% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/placebo_lm.R
\name{placebo_lm}
\alias{placebo_lm}
\title{Fit a placebo partial-identification model}
\usage{
placebo_lm(
  data,
  outcome,
  treatment,
  placebo,
  covariates = NULL,
  structure = c("placebo_outcome", "placebo_treatment", "observed_confounder_1",
    "observed_confounder_2", "post_outcome")
)
}
\arguments{
\item{data}{A data frame containing all analysis variables.}

\item{outcome}{Character. Name of the outcome variable (\code{Y} in the paper).}

\item{treatment}{Character. Name of the treatment variable (\code{D}).}

\item{placebo}{Character. Name of the placebo variable (\code{P}). Depending on
\code{structure} this is read as a placebo outcome, a placebo treatment, an
observed confounder, or a post-outcome variable.}

\item{covariates}{Character vector of observed covariate names (\code{X}) to
include in every regression. Defaults to \code{NULL} (intercept only).}

\item{structure}{Character. The assumed causal structure. One of
\code{"placebo_outcome"}, \code{"placebo_treatment"}, \code{"observed_confounder_1"},
\code{"observed_confounder_2"}, or \code{"post_outcome"}. See
\code{\link[=plm_structure_table]{plm_structure_table()}} for a description of each, and the
\code{structures} vignette for worked examples.}
}
\value{
An object of class \code{"placebo_lm"}: a list with components
\describe{
\item{\code{data}}{the analysis data frame}
\item{\code{vars}}{named list of variable roles (\code{Y}, \code{D}, \code{P}, \code{X})}
\item{\code{structure}}{the structure name}
\item{\code{spec}}{the corresponding entry of the structure registry}
\item{\code{formulas}}{the fitted regression formulas}
\item{\code{regressions}}{the fitted \code{lm} objects}
\item{\code{coefs}}{list with \code{target} and \code{sens}, each
\code{list(estimate=, se=, df=)}}
\item{\code{SF}}{the scale factor}
}
}
\description{
Sets up a partial-identification analysis of the effect of \code{treatment} on
\code{outcome}, leveraging an imperfect \code{placebo} variable, following Rohde and
Hazlett.

The regressions implied by the assumed causal \code{structure} are fitted once,
here, and every downstream calculation (\code{\link[=plm_estimate]{plm_estimate()}}, \code{\link[=plm_bounds]{plm_bounds()}},
\code{\link[=plm_solve]{plm_solve()}}, the plots) reads from the returned object. The scale factor
\code{SF} does not depend on the sensitivity parameters and so is also computed
once.
}
\examples{
set.seed(1)
n <- 500
U <- rnorm(n); X <- rnorm(n)
D <- X + U + rnorm(n)
P <- X + U + rnorm(n)
Y <- D + X + U + rnorm(n)
dat <- data.frame(Y = Y, D = D, P = P, X = X)

fit <- placebo_lm(dat, outcome = "Y", treatment = "D", placebo = "P",
                  covariates = "X", structure = "placebo_outcome")
fit

}
\references{
Rohde, A. and Hazlett, C. Causal progress with imperfect placebo treatments
and outcomes.
}

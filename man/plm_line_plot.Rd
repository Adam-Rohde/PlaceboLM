% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plm_line_plot}
\alias{plm_line_plot}
\title{Line plot of the adjusted estimate against relative confounding}
\usage{
plm_line_plot(
  fit,
  k = NULL,
  m = NULL,
  imperfection = 0,
  gran = 25,
  n_boot = 500,
  alpha = 0.05,
  ci_type = c("percentile", "normal"),
  benchmarks = TRUE,
  cores = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{placebo_lm} object.}

\item{k, m}{Numeric vector of length 2 giving the range to plot on the x-axis,
or a longer vector of explicit values. Supply exactly one. Defaults to
\code{k = c(0, 2)}.}

\item{imperfection}{Numeric vector of placebo-imperfection values; one panel
is drawn per value. Defaults to \code{0}.}

\item{gran}{Integer. Number of x-axis points per panel. Defaults to \code{25}.}

\item{n_boot}{Non-negative integer. Bootstrap replicates. Defaults to \code{500}.}

\item{alpha}{Numeric. Significance level. Defaults to \code{0.05}.}

\item{ci_type}{\code{"percentile"} or \code{"normal"}.}

\item{benchmarks}{Logical. Mark the benchmark estimates. Defaults to \code{TRUE}.}

\item{cores}{Integer. Cores for the bootstrap.}

\item{...}{Passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly, the data frame of plotted values.
}
\description{
Plots the partially identified estimate as a function of the
relative-confounding parameter, with a bootstrap confidence band, and marks
the three benchmark estimates. One panel is drawn per value of
\code{imperfection}.
}
\examples{
set.seed(1)
n <- 300
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + U + rnorm(n)
fit <- placebo_lm(data.frame(Y = Y, D = D, P = P),
                  "Y", "D", "P", structure = "placebo_outcome")
plm_line_plot(fit, k = c(0, 2), n_boot = 50)

}

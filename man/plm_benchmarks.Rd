% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{plm_benchmarks}
\alias{plm_benchmarks}
\title{Benchmark estimates}
\usage{
plm_benchmarks(fit, imperfection = 0)
}
\arguments{
\item{fit}{A \code{placebo_lm} object.}

\item{imperfection}{Numeric scalar. Postulated placebo imperfection.
Defaults to \code{0}.}
}
\value{
A data frame with columns \code{benchmark}, \code{k}, \code{m}, \code{imperfection},
and \code{estimate}.
}
\description{
Returns the three reference points the paper reports alongside every
analysis, as data rather than as plot annotations:

\describe{
\item{No unobserved confounding}{\code{k = 0}; the ordinary least-squares
estimate.}
\item{DID}{\code{m = 1}; conventional difference-in-differences, i.e. parallel
trends on the observed scale.}
\item{Equiconfounding}{\code{k = 1}; equal confounding after rescaling.}
}

All three assume a perfect placebo unless \code{imperfection} is given.
}
\examples{
set.seed(1)
dat <- data.frame(Y = rnorm(300), D = rnorm(300), P = rnorm(300))
fit <- placebo_lm(dat, "Y", "D", "P", structure = "placebo_outcome")
plm_benchmarks(fit)

}

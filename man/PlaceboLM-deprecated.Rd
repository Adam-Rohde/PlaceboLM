% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy.R
\name{PlaceboLM-deprecated}
\alias{PlaceboLM-deprecated}
\alias{placeboLM}
\alias{estimate_regs}
\alias{estimate_PLM}
\alias{boot_funk}
\alias{bootstrap_regs}
\alias{placeboLM_point_estimate}
\alias{placeboLM_table}
\alias{beta_expression_convert}
\alias{placeboLM_contour_plot}
\alias{placeboLM_line_plot}
\title{Deprecated pre-0.2.0 interface}
\usage{
placeboLM(
  data = "",
  placebo_data = NULL,
  outcome,
  treatment,
  placebo_outcome = "",
  placebo_treatment = "",
  DP = "",
  PY = "",
  observed_covariates = c("1"),
  partialIDparam_minmax = c(list(k = c(-2, 2), coef_P_D_given_XZ = c(-2, 2)))
)

estimate_regs(plm, dset_name = "", dset = NULL)

estimate_PLM(plm, partialIDparam, estimated_regs, returned)

boot_funk(boot_data, indys, plm, partialIDparam)

bootstrap_regs(plm, partialIDparam, n_boot)

placeboLM_point_estimate(
  plm,
  partialIDparam,
  bootstrap = TRUE,
  n_boot,
  alpha = 0.05
)

placeboLM_table(plm, n_boot, ptiles = c(0, 0.5, 1), alpha = 0.05, decimals = 3)

beta_expression_convert(t)

placeboLM_contour_plot(plm, gran = 100, decimals = 3)

placeboLM_line_plot(
  plm,
  bootstrap = TRUE,
  n_boot = 10,
  ptiles = c(0, 0.5, 1),
  focus_param = "k",
  ptile_param = "coef_P_D_given_XZ",
  gran = 10,
  alpha = 0.05,
  decimals = 3
)
}
\arguments{
\item{...}{Arguments of the corresponding pre-0.2.0 function. See the
package sources, or the appendix of the paper, for their meanings.}
}
\value{
As in the pre-0.2.0 interface.
}
\description{
These functions are the interface used by the example code printed in the
appendix of Rohde and Hazlett. They are retained, with their arithmetic
unchanged, so that the published code continues to run and to produce the
figures the paper reports.

They are deprecated. New work should use \code{\link[=placebo_lm]{placebo_lm()}} together with
\code{\link[=plm_estimate]{plm_estimate()}}, \code{\link[=plm_bounds]{plm_bounds()}}, \code{\link[=plm_solve]{plm_solve()}}, \code{\link[=plm_line_plot]{plm_line_plot()}}, and
\code{\link[=plm_contour_plot]{plm_contour_plot()}}, which return numerically identical estimates.

Two departures from the pre-0.2.0 behaviour: double placebos raise an error
rather than being estimated, and each entry point warns once per session.
}
\keyword{internal}

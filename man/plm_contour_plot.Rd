% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plm_contour_plot}
\alias{plm_contour_plot}
\title{Contour plot over relative confounding and placebo imperfection}
\usage{
plm_contour_plot(
  fit,
  k = NULL,
  m = NULL,
  imperfection = NULL,
  gran = 60,
  nlevels = 20,
  benchmarks = TRUE,
  ...
)
}
\arguments{
\item{fit}{A \code{placebo_lm} object.}

\item{k, m}{Numeric vector of length 2, the x-axis range. Supply exactly one.
Defaults to \code{k = c(0, 2)}.}

\item{imperfection}{Numeric vector of length 2, the y-axis range. Defaults to
a symmetric range around zero scaled to the sensitivity coefficient.}

\item{gran}{Integer. Grid points per axis. Defaults to \code{60}.}

\item{nlevels}{Integer. Number of contour levels. Defaults to \code{20}.}

\item{benchmarks}{Logical. Mark the benchmark estimates. Defaults to \code{TRUE}.}

\item{...}{Passed to \code{\link[graphics:contour]{graphics::contour()}}.}
}
\value{
Invisibly, a list with \code{x}, \code{y}, and \code{z}.
}
\description{
Draws the adjusted estimate as a contour surface over the two sensitivity
parameters, with the zero contour highlighted -- the set of assumptions at
which the sign of the estimated effect changes.
}
\examples{
set.seed(1)
n <- 300
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + U + rnorm(n)
fit <- placebo_lm(data.frame(Y = Y, D = D, P = P),
                  "Y", "D", "P", structure = "placebo_outcome")
plm_contour_plot(fit)

}

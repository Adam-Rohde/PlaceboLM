% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate.R
\name{plm_bounds}
\alias{plm_bounds}
\title{Bounds on the effect over a range of assumptions}
\usage{
plm_bounds(
  fit,
  k = NULL,
  m = NULL,
  imperfection = 0,
  n_boot = 1000,
  alpha = 0.05,
  ci_type = c("percentile", "normal"),
  cores = NULL,
  engine = c("lm", "matrix")
)
}
\arguments{
\item{fit}{A \code{placebo_lm} object.}

\item{k}{Numeric vector of length 2, \code{c(min, max)}. Supply exactly one of
\code{k} or \code{m}.}

\item{m}{Numeric vector of length 2, \code{c(min, max)}.}

\item{imperfection}{Numeric vector of length 1 or 2. A single value fixes the
placebo imperfection; two values give a range. Defaults to \code{0} (perfect
placebo).}

\item{n_boot}{Non-negative integer. Bootstrap replicates. \code{0} skips
uncertainty quantification and returns the point bound only.}

\item{alpha}{Numeric. Significance level for the bootstrap interval.
Defaults to \code{0.05}.}

\item{ci_type}{\code{"percentile"} (default) or \code{"normal"}. The paper notes both
the nonparametric bootstrap and the normal approximation to bootstrap
standard errors are available.}

\item{cores}{Integer. Cores for the bootstrap. Defaults to one less than
detected, minimum 1.}

\item{engine}{\code{"lm"} (default) or \code{"matrix"}; see \code{\link[=plm_grid]{plm_grid()}}.}
}
\value{
A one-row data frame with columns \code{k_low}, \code{k_high}, \code{m_low},
\code{m_high}, \code{imperfection_low}, \code{imperfection_high}, \code{lower}, \code{upper}, and
-- when \code{n_boot > 0} -- \code{lower_boot_q} and \code{upper_boot_q}.
}
\description{
The headline output of the method. Given a \emph{range} of relative confounding
(and optionally a range of placebo imperfection), returns the interval of
adjusted estimates consistent with those assumptions -- the package
equivalent of the paper's "assuming only that 0.5 < k < 1 places the results
between -1,249 USD and +3,428 USD."

The estimator is linear in \code{k} and in \code{imperfection}, so the extremes over a
rectangular assumption region are always attained at its corners; the bound
is computed exactly rather than by searching a grid.

When \code{n_boot > 0}, the bound is recomputed on each bootstrap resample and the
reported interval widens to reflect sampling uncertainty \emph{in the bound
itself}: \code{ci_lower} is the lower quantile of the bootstrapped lower bounds
and \code{ci_upper} the upper quantile of the bootstrapped upper bounds.
}
\section{What the bootstrap columns are, and are not}{

Four distinct objects are easy to confuse here.
\enumerate{
\item A confidence interval for the estimate at one fixed choice of the
sensitivity parameters -- that is \code{\link[=plm_grid]{plm_grid()}} or \code{\link[=plm_analytic]{plm_analytic()}}.
\item The point bounds: the range of estimates over the assumption region,
holding the data fixed. These are \code{lower} and \code{upper}.
\item Bootstrap uncertainty about each bound separately. These are
\code{lower_boot_q} and \code{upper_boot_q}: the lower quantile of the bootstrapped
lower bounds, and the upper quantile of the bootstrapped upper bounds.
\item A confidence region with a coverage guarantee for the whole
identified set, in the sense of Imbens and Manski (2004).
}
This package provides the first three. It does \strong{not} provide the fourth,
and \code{lower_boot_q}/\code{upper_boot_q} should not be reported as though it did.
They are a descriptive summary of sampling variability in the bounds, not a
procedure with an established coverage property.
}

\examples{
set.seed(1)
n <- 400
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + U + rnorm(n)
fit <- placebo_lm(data.frame(Y = Y, D = D, P = P),
                  "Y", "D", "P", structure = "placebo_outcome")

plm_bounds(fit, k = c(0.5, 1), n_boot = 0)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{plm_grid}
\alias{plm_grid}
\title{Bootstrap confidence intervals over a sensitivity grid}
\usage{
plm_grid(
  fit,
  k = NULL,
  m = NULL,
  imperfection = 0,
  n_boot = 1000,
  alpha = 0.05,
  ci_type = c("percentile", "normal"),
  cores = NULL
)
}
\arguments{
\item{fit}{A \code{placebo_lm} object.}

\item{k, m}{Numeric vectors of relative-confounding values. Supply exactly one.}

\item{imperfection}{Numeric vector of placebo-imperfection values. Crossed
with \code{k}/\code{m} to form the grid. Defaults to \code{0}.}

\item{n_boot}{Non-negative integer. Bootstrap replicates. \code{0} returns point
estimates only.}

\item{alpha}{Numeric. Significance level. Defaults to \code{0.05}.}

\item{ci_type}{\code{"percentile"} (default) or \code{"normal"}.}

\item{cores}{Integer. Cores for the bootstrap.}
}
\value{
A data frame with one row per grid point and columns \code{k}, \code{m},
\code{imperfection}, \code{estimate}, and -- when \code{n_boot > 0} -- \code{std_error},
\code{ci_lower}, \code{ci_upper}.
}
\description{
Evaluates the adjusted estimate with a bootstrap confidence interval at every
requested combination of the relative-confounding parameter and the placebo
imperfection. Resampling is performed once and reused across the whole grid.
}
\examples{
set.seed(1)
n <- 300
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + U + rnorm(n)
fit <- placebo_lm(data.frame(Y = Y, D = D, P = P),
                  "Y", "D", "P", structure = "placebo_outcome")

plm_grid(fit, k = c(0, 0.5, 1), n_boot = 50)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulate.R
\name{plm_compare_structures}
\alias{plm_compare_structures}
\title{Compare structural readings of one placebo}
\usage{
plm_compare_structures(
  data,
  outcome,
  treatment,
  placebo,
  covariates = NULL,
  structures = names(plm_structures),
  k = 1,
  imperfection = 0
)
}
\arguments{
\item{data, outcome, treatment, placebo, covariates}{As in \code{\link[=placebo_lm]{placebo_lm()}}.}

\item{structures}{Character vector of structures to compare. Defaults to all
supported ones.}

\item{k}{Numeric scalar at which to report a comparable estimate. Defaults to
\code{1} (equiconfounding after rescaling). \code{k} is used rather than \code{m} because
\code{m} is not comparable across structures with different scale factors.}

\item{imperfection}{Numeric scalar. Defaults to \code{0}.}
}
\value{
A data frame with one row per structure and columns \code{structure},
\code{paper_ref}, \code{sens_param}, \code{SF}, \code{at_k0}, \code{at_k1}, \code{m_equals_1}, and
\code{tipping_k}.
}
\description{
Fits the same outcome, treatment and placebo under several assumed causal
structures and tabulates what each implies. Useful where a graph admits more
than one reading -- the paper's Table 2\link{c} can be analysed either as an
imperfect placebo treatment or as an observed confounder -- and the
comparison is itself informative.

The structures are \emph{different assumptions}, not competing estimates of one
quantity. Where they disagree, the disagreement measures how much the
conclusion depends on the causal reading, which is not something the data can
settle.
}
\examples{
set.seed(1)
n <- 400
U <- rnorm(n); D <- U + rnorm(n)
P <- U + rnorm(n); Y <- D + P + U + rnorm(n)
dat <- data.frame(Y = Y, D = D, P = P)

plm_compare_structures(dat, "Y", "D", "P")

}

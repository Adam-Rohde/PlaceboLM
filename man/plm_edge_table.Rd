% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edges.R
\name{plm_edge_table}
\alias{plm_edge_table}
\title{How causal assumptions map to structures}
\usage{
plm_edge_table()
}
\value{
A data frame with columns \code{edges}, \code{placebo_role}, \code{structure}, and
\code{paper_ref}.
}
\description{
The resolution table used by \code{\link[=placebo_lm]{placebo_lm()}} when a structure is described
with \code{edges} rather than named directly. Edges are written between the roles
\code{D} (treatment), \code{P} (placebo) and \code{Y} (outcome).

Two rows share an edge set: with no direct edges, and with \code{P->Y}, the
placebo admits two readings and \code{placebo_role} selects between them. The
second of those is the paper's own observation that its Table 2\link{c} graph can
be analysed either as an imperfect placebo treatment or as an observed
confounder.

The combination \code{D->P} plus \code{P->Y} makes the placebo a mediator, which the
paper does not recommend; requesting it raises an error.
}
\examples{
plm_edge_table()

}

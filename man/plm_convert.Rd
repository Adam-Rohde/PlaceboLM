% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{plm_convert}
\alias{plm_convert}
\alias{plm_m_to_k}
\alias{plm_k_to_m}
\title{Convert between the m and k parameterizations}
\usage{
plm_m_to_k(fit, m)

plm_k_to_m(fit, k)
}
\arguments{
\item{fit}{A \code{placebo_lm} object from \code{\link[=placebo_lm]{placebo_lm()}}.}

\item{m, k}{Numeric vector of parameter values to convert.}
}
\value{
A numeric vector.
}
\description{
\code{plm_m_to_k()} and \code{plm_k_to_m()} convert between the two relative-confounding
parameterizations used in the paper, using the scale factor of a fitted model.
They are related by \code{m = k * SF}.
}
\examples{
set.seed(1)
dat <- data.frame(Y = rnorm(200), D = rnorm(200), P = rnorm(200))
fit <- placebo_lm(dat, "Y", "D", "P", structure = "placebo_outcome")

# Conventional difference-in-differences is m = 1
plm_m_to_k(fit, 1)

}
